# Health web routes

These thin App Router files compose the feature-owned Health screens from `src/features/health`. The dashboard composition root supplies one stable authenticated owner and the shared Supabase repository set. Tests and the explicit local development bypass use isolated memory storage.
