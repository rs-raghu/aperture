# Education

Route files remain thin and delegate to `src/features/education`. The dashboard composition root supplies the authenticated owner's shared Supabase repository; tests and the explicit local development bypass can still supply an isolated memory runtime.
