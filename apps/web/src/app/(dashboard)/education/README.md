# Education

Phase 9 implements the Education-only development preview here. The shared layout owns one in-memory composition root for all nested Education routes; a browser refresh resets it. Route files remain thin and delegate to `src/features/education`.
