# Future Supabase backend

This directory reserves the future Supabase PostgreSQL and Auth boundary. Implementation begins no earlier than Phase 26.

Phase 4 contains no client initialization, SQL, migrations, policies, functions, seed data, project references, or credentials. Public signup is intended to be disabled when authentication is implemented. Every table containing personal data will require owner-based Row Level Security before use.
