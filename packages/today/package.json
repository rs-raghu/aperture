{
  "name": "@aperture/today",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "pretest": "npm run build --workspace @aperture/planner && npm run build --workspace @aperture/today",
    "test": "vitest run",
    "lint": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aperture/education": "0.7.0",
    "@aperture/finance": "0.4.0",
    "@aperture/health": "0.4.0",
    "@aperture/planner": "0.1.0"
  }
}
