{
  "name": "@aperture/finance-memory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "pretest": "npm run build --workspace @aperture/validation && npm run build --workspace @aperture/finance && npm run build --workspace @aperture/finance-memory",
    "test": "vitest run",
    "build": "tsc --project tsconfig.build.json",
    "lint": "tsc --project tsconfig.json --noEmit --noUnusedLocals --noUnusedParameters"
  },
  "dependencies": {
    "@aperture/finance": "0.4.0",
    "@aperture/validation": "0.5.0"
  }
}
