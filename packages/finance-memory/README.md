# @aperture/finance-memory

Volatile, per-factory Finance repository adapters for tests and local previews. Records are owner-scoped, defensively cloned, deterministically ordered, and paginated with mutation-sensitive cursors. No data is shared between factory instances or persisted across process restarts.
