{
  "name": "@aperture/health-memory",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    },
    "./adapter-internals": {
      "types": "./src/adapter-internals.ts",
      "import": "./dist/adapter-internals.js"
    }
  },
  "scripts": {
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "pretest": "npm run build --workspace @aperture/validation && npm run build --workspace @aperture/health && npm run build --workspace @aperture/health-memory",
    "test": "vitest run",
    "build": "tsc --project tsconfig.build.json",
    "lint": "tsc --project tsconfig.json --noEmit --noUnusedLocals --noUnusedParameters"
  },
  "dependencies": {
    "@aperture/health": "0.4.0"
  }
}
