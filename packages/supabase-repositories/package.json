{
  "name": "@aperture/supabase-repositories",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": {
      "types": "./src/index.ts",
      "import": "./dist/index.js"
    }
  },
  "scripts": {
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "pretest": "npm run build --workspace @aperture/postgres-repositories && npm run build --workspace @aperture/supabase-repositories",
    "test": "vitest run",
    "build": "tsc --project tsconfig.build.json",
    "lint": "tsc --project tsconfig.json --noEmit"
  },
  "dependencies": {
    "@aperture/finance-memory": "0.1.0",
    "@aperture/postgres-repositories": "0.1.0",
    "@supabase/supabase-js": "^2.114.0"
  }
}
